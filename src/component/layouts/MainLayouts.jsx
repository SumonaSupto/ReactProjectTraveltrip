import React from 'react';
import Navbar from '../Navbar';
import Hero from '../Hero';

const MainLayouts = () => {
    return (
        <>
        <Navbar/>
        <Hero/>
        </>
    );
};

export default MainLayouts;